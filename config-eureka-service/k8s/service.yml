apiVersion: v1
kind: Service
metadata:
  name: config-eureka-service
  labels:
    app:config-eureka-service
spec:
  type: ClusterIP
  selector:
    app: config-eureka-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8888